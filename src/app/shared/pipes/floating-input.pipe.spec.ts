import { FloatingInputPipe } from './floating-input.pipe';

describe('FloatingInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FloatingInputPipe();
    expect(pipe).toBeTruthy();
  });
});

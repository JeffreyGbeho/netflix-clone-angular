import { FloatingInputLabelDirective } from './floating-input-label.directive';

describe('FloatingInputLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingInputLabelDirective();
    expect(directive).toBeTruthy();
  });
});

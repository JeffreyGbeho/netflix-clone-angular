import { MovieCardHoverDirective } from './movie-card-hover.directive';

describe('MovieCardHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieCardHoverDirective();
    expect(directive).toBeTruthy();
  });
});
